node_modules/
.DS_Store
.idea/
.vscode/
coverage/
dist/
build/
tmp/
*.log
*.tsbuildinfo
# This is the AgentLoops tool repo itself; never commit a local ledger created
# by running the CLI/demo here (demo writes to a temp dir, but guard anyway).
.agentloops/

# local env
.env
.env.local
.env.*.local

# keep generated assets if any
*.sqlite
*.sqlite3
